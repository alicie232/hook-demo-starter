import { Button } from "./Button";

const config = {
  component: Button,
  title: "Button",
};

export const Default = () => <Button title="Default" />;
export const Test = () => <Button title="Test" />;

export default config;
